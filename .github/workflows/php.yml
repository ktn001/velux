name: "PHP"

on:
  push:
    branches:
      - master
    paths:
      - "**.php"
  pull_request:
    branches:
      - master
    paths:
      - "**.php"

jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: michaelw90/PHP-Lint@master

      
